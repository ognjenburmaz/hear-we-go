<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name"/>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://loki:3100/loki/api/v1/push</url>
        </http>
        <batchMaxItems>3</batchMaxItems>
        <batchTimeoutMs>500</batchTimeoutMs>
        <format>
            <label>
                <pattern>app=${appName},host=${HOSTNAME},level=%level</pattern>
            </label>
            <message class="com.github.loki4j.logback.JsonLayout">
                <timestampFieldName>time</timestampFieldName>
                <messageFormatter class="com.github.loki4j.logback.MessageFormatter">
                    <pattern>%msg %nopex</pattern>
                </messageFormatter>
            </message>
        </format>
    </appender>

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="OFF">
        <appender-ref ref="LOKI"/>
    </root>

    <logger name="com.streaming.users.controller.UserController" level="INFO"/>

    <logger name="com.streaming.users.security.JwtFilter" level="INFO"/>

    <logger name="com.streaming.users.exception.GlobalExceptionHandler" level="INFO"/>

</configuration>
