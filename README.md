# Hear We Go!

Open Git Bash in root folder and run `choco install mkcert` then `sh generate-ssl.sh`

run ```docker-compose up --build``` (or ```docker compose up --build``` in Linux enviroments) in root to start all
services.